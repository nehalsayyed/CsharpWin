name: Build

on: 
   workflow_dispatch:

jobs:
   build:
    runs-on: ubuntu-latest
    steps:
       - run: |
          git clone https://github.com/apache/fineract.git
          cd fineract
          chmod 777 gradlew 
          ./gradlew clean bootJar
