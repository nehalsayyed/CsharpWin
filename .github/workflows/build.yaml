name: Build

on: workflow_dispatch

jobs:
  build:
     runs-on: ubuntu-latest
     steps:
     - run: |
           git clone https://github.com/Klairm/chip8.git
           cd chip8
           chmod 777 install.sh
           ./install.sh 
           2
