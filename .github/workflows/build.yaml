name: Build

on: workflow_dispatch

jobs:
  build:
     runs-on: ubuntu-latest
     steps:
     - run: |
           git clone https://github.com/QiZD90/chip-8.git
           cd chip-8
           gradle build
