name: Build

on:
  workflow_dispatch:

jobs:
  build-android:
    name: Build
    runs-on: ubuntu-latest
    container: rust:latest 

    steps:
      

      - name: Run
        run: |
             cargo version
