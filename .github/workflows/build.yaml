name: Build

on: 
   workflow_dispatch:

jobs:
   build:
    runs-on: ubuntu-latest
    steps:
       
       
    - name: Set up Java
      uses: fabasoad/pascal-action@v1
      
       
       
    - run: |
          fpc -V
