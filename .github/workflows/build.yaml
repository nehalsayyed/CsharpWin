name: Build

on: 
   workflow_dispatch:

jobs:
   build:
    runs-on: ubuntu-latest
    steps:
       - run: |
          git clone https://github.com/ybkuroki/go-webapp-sample.git
          cd go-webapp-sample
          docker build -t app .
          docker run app
