name: Build CompCert ARM64 for Termux PRoot

on:
  
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest # Native ARM64 GitHub Runner

    steps:
      - name: Flutter action
        uses: subosito/flutter-action@v2.23.0
      - name: Install Build Toolchain & Dependencies
        run: |
            flutter doctor 
            flutter --version 
          
