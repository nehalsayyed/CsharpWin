name: Build

on: workflow_dispatch

jobs:
  build:
     runs-on: ubuntu-24.04-arm
     container: termux/termux-docker:aarch64 


     steps:
       - name: Running
         run: |
            pkg update -y
            pkg upgrade -y
            pkg i fastfetch -y
            fastfetch
